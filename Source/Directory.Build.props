<Project>
    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

    <PropertyGroup>
        <RootNamespace>LP.Common.$(MSBuildProjectName)</RootNamespace>
    </PropertyGroup>
</Project>
